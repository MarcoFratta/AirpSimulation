package org.VoituLex.test;

import org.VoituLex.model.Airport;
import org.VoituLex.model.Direction;
import org.VoituLex.model.Ticket;
import org.VoituLex.view.SimulationView;
import org.junit.Test;

import java.util.Random;

public class PathTest {




    @Test
    public void main(){


      /*  Airport airport = new Airport(3,5);
        airport.init(bots ->{
            Random r = new Random();
            bots.forEach(b-> b.setTicket(new Ticket(r.nextInt(21),r.nextInt(4), Direction.UP)));
        });
        SimulationView view = new SimulationView(airport);
        view.createRender();
        */
    }
}
